package Spellbook::Helpe::Nmap_Parser {
    use strict;
    use warnings;

    sub new {
        my ($self) = @_;
        
        # https://metacpan.org/pod/Nmap::Parser

        if (1) {

        }

        return 0;
    }
}

1;