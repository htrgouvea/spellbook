package Spellbook::Mail::Checker {
    use strict;
    use warnings;

    our $VERSION = '0.0.1';

    sub new {
        my ($self) = @_;
        
        # SPF
        # DMARC
        # DKIM

        return 0;
    }
}

1;